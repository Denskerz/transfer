Давайте разберем ваш запрос по пунктам.

1. Пререквизиты:
   а) Установленный и керберизированный кластер Hadoop
      - Кластер Hadoop должен быть установлен и сконфигурирован. Он должен использовать Kerberos для аутентификации пользователей и компонентов кластера.
   б) Установленный кластер OpenSearch
      - Кластер OpenSearch должен быть установлен и запущен.
   в) 3 сервера для компонентов графовой платформы: FastGraph, BigGraph, GSS-REST
      - Для работы графовой платформы необходимы 3 отдельных сервера, на которых будут развернуты следующие компоненты:
        - FastGraph
        - BigGraph
        - GSS-REST

2. Установка Hadoop кластера
   а) Сделать локальный репозиторий с пакетами сборки Hadoop и Ambari и прописать его в настройках всех хостов кластера:
      - Необходимо создать локальный репозиторий, который будет содержать пакеты для установки Hadoop и Ambari.
      - Для этого потребуются следующие пакеты:
        - nginx - веб-сервер для хостинга репозитория
        - createrepo - утилита для создания и обновления YUM-репозиториев
        - yum-utils - набор инструментов для работы с YUM
      - Создается папка для хранения репозитория:
        ```
        mkdir -p /usr/share/nginx/html/repos/7/{os,updates}/x86_64
        ```
      - Создаются сами репозитории:
        ```
        createrepo -v /usr/share/nginx/html/repos/7/os/x86_64
        createrepo -v /usr/share/nginx/html/repos/7/updates/x86_64
        ```
      - Далее настраивается nginx для доступа к репозиторию:
        ```
        vi /etc/nginx/conf.d/default.conf
        ```
        Добавляется конфигурация:
        ```
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            autoindex on;
        }
        ```
      - Перезапускается nginx:
        ```
        sudo systemctl restart nginx
        ```
      - Создается файл с настройкой репозитория:
        ```
        vi /etc/yum.repos.d/local.repo
        ```
        Добавляется содержимое:
        ```
        [local]
        name=Local Yum Repo
        baseurl=http://192.168.0.10/repos/$releasever/os/$basearch/
        enabled=1
        gpgcheck=0
        ```
      - Теперь локальный репозиторий готов и его можно использовать для установки Hadoop и Ambari на все хосты кластера.

Резюмируя, вы создали локальный YUM-репозиторий с пакетами Hadoop и Ambari, настроили nginx для его публикации и прописали настройки репозитория в конфигурационный файл yum.